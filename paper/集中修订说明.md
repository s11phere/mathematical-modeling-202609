# 论文集中修订说明

修订日期：2026-09-13。基于远程 `main` 的 `7ef5b67`，主文稿现位于 `paper/`，支撑材料位于 `attachments/`。原审查报告保留在 `main_论文审查报告.md`，本说明记录结合新版论文实际情况作出的处理。

## 处理范围

| 范围 | 本次处理 |
| --- | --- |
| 问题一 | 补全无界性证明中的收敛子列与极限论证，说明圆周角度对齐和复杂度；重绘局部放大图，保留角楔颜色、边界及中心射线，避免遮挡测站；修复表注排版。 |
| 问题二与正式测试 | 保留远程数据和正式测试表。终审核对发现接收门槛仍按测站到整段源位的最短距离广播，未逐源位筛选；按用户确认，将论文统一限定为中心方位近似下的几何布点估价，不再称为真实接收条件期望。修正近轴近似、无界性、尺度律和 60 m 阈值解释，更新数据字段说明及其中的旧最小值。 |
| 问题三 | 定义角楔、估计中心和包围半径，说明 301 点加权、回退规则、真实清除动作以及物理误差和数值余量。区分点网格检查、连续证书和启发式退出；将摘要的主要比较改为相同连续证书标准下 joint 相对 adaptive 节省 8.4%，95% 配对区间为 [6.9, 9.8]%，50 组随机案例。保留相对 field 的 42.8%，明确仅为不同停止标准下的退出成本比较。 |
| 问题四 | 补充任意朝向覆盖、圆弧并集证书和双侧截断的推导；澄清程序退出不等于完成认证。按实现修正侧向访问次序、中心清除触发条件和失败后处理；解释后验规划近似、方向换算及回退。明确 1024 个全定向参考状态仅用于航路估价，信息代价消融尚不能证明稳定提速。 |
| 统计与图表 | 保留全部历史实验档案；正文移除速度档比较，分析范围为 180 次主/压力实验加 20 次消融，共 200 次、59630 条动作；完整历史档案为 230 次、65173 条动作。同步结果图、消融图、图内时间符号和长表续页标识。 |
| 文献 | 形成 9 条文献并按首次引用顺序编号；更正 Bishop 的题名和期页、Galceran 的合作者姓名，合并 Toussaint 重复记录。Carlsson–Jia、Song–Zhang 两条缺乏可核实出处，未纳入。详见 `references_核对说明.md`。 |
| 复现与版式 | 问题四图表支持指定新结果目录；修正 Windows 路径比较和清单排序，保留冻结算法来源哈希。附件禁用 Git 换行转换，当前清单使用固定 LF。参考文献与附录分开分页，修正字体回退并检查最终页面。 |

## 保留的边界

没有修改定位、搜索、清除或航路策略的实现，没有重跑主实验，也没有改写冻结输入、结果或正式测试记录。本次仅调整论述、图件、制图/制表入口和交付校验工具。审查报告中已由远程更新解决的问题不再重复处理；会引入大量实验的先验或启发式调整，以明确适用条件和证据限度处理。

## 验证结果

- LaTeX 编译成功；无缺字、失效引用、重复标签或溢出框。摘要为首页单页。
- 最终 `main.pdf` 共 39 页，参考文献结束于第 30 页，附录从第 31 页开始；已目视检查关键公式、图表、续表及分页。
- 自动核对 9 条文献的首次引用顺序与参考文献表一致。
- 当前附件清单、全部交付哈希及正文/附件插图一致；四题包校验通过。问题四冻结策略依赖仍按原始来源哈希核验；使用独立临时输入目录检查图表入口，确认轨迹来源和制表输入选择有效。
- 已检查修改的 Python 文件语法、Git 差异格式及冻结数据/策略未发生内容变更；未把包一致性校验当作新的性能实验。
