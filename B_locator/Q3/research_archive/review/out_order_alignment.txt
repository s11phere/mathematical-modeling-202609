60 局：排序决策 822 次，目标执行 470 次

A. 排序决策时刻的中心 vs 执行时刻的中心
   不一致的样本 470/470（其余 0 个在执行时已无有界区域）
   中心漂移：均值 0.0 m，中位数 0.0 m，P90 0.0 m，最大 0.0 m
   发生漂移（>0）的比例：0/470 = 0.0%
   '走到中心'的距离：决策时预计均值 778 m，实际 778 m，差均值 0 m

B. 清除成功的位置 vs 逼近目标（说明'走到中心'这一手有多准）
   成功清除 306 次；清除点到执行时中心：均值 16.1 m，中位数 11.9 m，P90 36.0 m
   （下面按局算清除点到真源的距离）
   清除点到真源：均值 4.2 m，中位数 3.2 m，P90 9.2 m（清除半径 20 m）

C. 排序结果的第一步，是否等于'离当前位置最近'的目标
   非最近邻开头的比例：82/580 = 14.1%（2-opt 与最近邻不一致的次数）

D. 每个目标被尝试清除时的各项代价分解（按结果分组）
   成功          306 次：实际耗时均值    147 s，实际行程均值    675 m（=   135 s 行程）
   失败/放弃       164 次：实际耗时均值      2 s，实际行程均值     10 m（=     2 s 行程）

   总实际耗时：成功 44899 s，失败 349 s
   目标执行占用合计 45248 s / 60 局 = 754 s/局（预算 1200 s/局）
