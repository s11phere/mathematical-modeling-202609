# 问题三：源分支说明摘录

摘自 `problem_b` 的 `B_locator/README.md`，运行路径已适配 `B_locator/Q3`。原始完整文档保存在 [来源快照](docs/source/B_locator/README.md)；源分支中未入库的报告与运行产物见 [迁入说明](README.md)。

## 问题 3：基于期望的行动（策略 + 算法 + 演练结果）

> 完整设计、关键坑、灵敏度与消融表、上界分析见 **`review/P3-design.md`**。

**策略（一句话）**：把"下一步去哪测"变成全局网格上的标量场
**score = 归一化期望定位质量 + λ·距离**，取 argmin 作为下一个检测点；对已经测到
≥2 条方位的频道，用问题 1 求定位区域，**滚动时域动态规划**航路依次去清除，清完继续扫描。

| 环节 | 做法 |
|---|---|
| 全局网格 | 靶区内 100 m 步长（`--grid-step`），每格记录一步的好坏 |
| 期望来源 | **复用问题 2 的网格模拟结果** `E[D\|可检测]`（`out/p3_family/`） |
| 变换 + 插值 | 刚体变换不变 ⇒ `b = R(-θ)(g-S)`，双线性插值查基准图；**按先验外径 t_hi 分族**（否则边界几何差 2–4 倍） |
| 未覆盖格子 | 一律取 1500 m（"最差位置"），超阈值同样截断到 1500 |
| 归一化 | `E_norm = mean_k E_k / 1500 ∈ [0,1]` |
| 距离层 | `+ λ·d/(2R_arena)`，λ 可调（默认 1.5；λ=0 时平均定位清除时间 649 s、行程 53 km，是 1.5 时的 1.6/2.3 倍） |
| 跳过规则 | 已清除频道、已定位两次的频道、同一有效接收半径内重复测过的频道都不再测 |
| 定位 | 问题 1 的 `analyse`：多边形区域 + 最小包围圆；真源必在区域内（自检 S5） |
| 清除 | 逼近 + 侧移精定位 + **覆盖式试探**（六边形格点间距 34 m ≤ 20√3，候选点不截断 ⇒ 必中）；预算不足时记"未尝试"而非"清除失败" |
| 航路 | **滚动时域动态规划**：每清一个目标就重读台账、以当前位置为起点、用最新区域中心重排（最近邻 + 2-opt）；**途中新定位的频道立刻插队**；不按预估代价裁剪航路（裁剪掉 2.5 个百分点） |

**离线演练（30 局随机案例，`python src/p3_run.py --mode mock --cases 30`）**

| 指标 | 数值 |
|---|---|
| 被清除干扰源个数的比例 | **0.995**（362/364） |
| 平均定位清除时间 | **314.6 s** |
| 平均清除个数 | 12.07（最少 10、最多 16） |
| 虚拟时间 / 行程 / 检测次数 | 5270 s / 20 508 m / 186.0 次 |
| 超时拒动 | 0 次 |
| 程序运行 | 0.35 s/局（纯策略计算，不含网络） |

**真实模拟器演练（2026-09-11，2 局，`--mode live`）**

| | 第 1 局（旧参数 gap=450） | **第 2 局（新参数 gap=1000）** |
|---|---|---|
| 案例编码 | `ZAXN-3GWG-GVFP-YMYD` | `5D2C-4E2F-V8W6-MVTN` |
| 干扰源总数 / 被清除 | 10 / **10** | 14 / **14** |
| **清除比例** | **1.000** | **1.000** |
| **平均定位清除时间** | 642.9 s | **239.5 s** |
| 虚拟时间 / 墙钟 | 6914 s / 6.47 s | 5286 s / 2.70 s |
| 检测次数 / 行程 | 384 / 22 152 m | **164** / 21 169 m |
| 清除失败 / 拒动 / 重试 | 0 / 0 / 0 | 2（均自动恢复）/ 0 / 0 |

**两局合计：24 个源全部清除（比例 1.000），平均定位清除时间 407.6 s。**

> **口径修正（重要）**：真实模拟器的**虚拟世界限时是 360000 s**，1200 s 只是
> **现实时间**预算。旧版 `MockArena` 把 1200 s 当成虚拟时间上限，等于给离线演练
> 加了一条真实测试里不存在的约束，导致离线"清除率只有 41.5%、平均 222.8 s"
> 的假象。改正后同一批 30 局为 **0.995 / 314.6 s**。
> 参数 `nosignal_gap_m` 也据此从 450 m 调整为 **1000 m**（= 有效接收半径下界），
> 在线实测第 2 局随即从 642.9 s 降到 **239.5 s**。
> 完整对比见 **`review/live-practice-20260911.md`**。

> **瓶颈分析**：两局演练都全清，但**第 2 局的 14 个源在 3353 s 就清完了，程序又跑了
> 1932 s（37%）才收工**（题目只给"10–16 个"的范围，算法无法确知已清完）；
> 第 1 局则有 3114 s（45%）花在"场上一条方位都没有"的空场搜索上。
> 这两条已经在"覆盖扫圈"（见下）与"统一滚动航路"（`src/p3_tour.py`）里解决：
> 空转占比从 29–37% 降到 ≈0。

### 问题 3 的第二套策略：覆盖扫圈（`--policy sweep`）

上面那条"改进方向"已经实现并离线验证：**原点已扫过 ⇒ 半径 1000 m 内不可能有源 ⇒
只需扫环带 r∈[1000,1800]，一圈 r=1100 m、8 个站位即可**（环带最大未覆盖 853 m < 1000 m，
**可证明完备**）。航线就是用户描述的形状：原点粗扫 → 走到某一站 → 沿同一方向绕一圈
（沿途顺手清除）→ 走满一圈即断定"靶区无残留源"并主动 `/exit`。

| 60 局离线（两批各 30） | 清除率 | 平均定位清除时间 | 虚拟时间 | 检测 | 行程 |
|---|---|---|---|---|---|
| 期望场贪心（`field`） | 0.9973 | **307.9 s** | 5295 s | 184.5 | 20 670 m |
| **覆盖扫圈（`sweep`）** | **0.9959** | 340.5 s | **4141 s** | **145.0** | **16 058 m** |

即：**清除率打平，虚拟时间/检测/行程少 20%+，并且清完后的空转从 29–37% 降到 ≈0**；
代价是按表 1 口径的平均定位清除时间略差（因为要跑完一圈才开始清）。
完整设计、三个实现坑与逐场景对照见 **`review/p3-sweep-design.md`**。

```bash
python src/p3_run.py --mode mock --cases 30 --policy sweep   # 覆盖扫圈
python src/p3_run.py --mode mock --cases 30 --policy field   # 期望场贪心
python src/p3_sweep.py --selfcheck                            # C1–C3 覆盖性自检
```

### 问题 3 的第三套策略：统一滚动航路（`--policy tour`，对照基线）

扫圈虽然把虚拟时间降了 20%，但结构是**"先绕完一圈、再统一清除"**——绕圈的 6.7 km
与清除航路的 5.2 km 是两段互不重叠的路，实测行程 16.8 km，是"预知全部源位后的
最优航路"（L2 = 10.7 km）的 **1.57 倍**。`src/p3_tour.py` 把两者合成**一条滚动时域
的统一航路**：

```
每个决策点重算一次航路，只执行第一个停点，然后重规划
  航路 = ① 环带骨架停点（r≈1000 的 8 个等分点，角向单调推进 ⇒ 结构性不折返）
         ↑ 目标（清除 / 定位）用"最便宜插入"塞进骨架的空隙里
  目标 = 已定位频道的最新定位区域中心
         只有一条方位 / 区域无界或"长条形"的频道（横向补测点）
```

于是航线就是**锯齿状的一圈**：原点出发 →（期望场）选第二个检测点、顺手清掉靠内圈
的目标 → 就近切入环带 → 沿一个方向绕一圈、边走边清 → 走完一圈即收工；
**覆盖判据逐频道计算**（用该频道真实测过的位置），所以顺路清除时做的检测会自动
抵扣掉后面的骨架停点，不会重复走。

| 30 局（`20260913+100i`） | 期望场贪心 | 覆盖扫圈 | **统一航路（默认）** |
|---|---|---|---|
| 被清除干扰源个数的比例 | 0.9945（28/30 全清） | 0.9780（23/30 全清） | **1.0000（30/30，364/364）** |
| **平均定位清除时间**（全程 ÷ 清除数） | 436.6 s | 357.5 s | **329.9 s** |
| 平均定位清除时间（清完即停表口径） | **314.6 s** | 352.4 s | 335.4 s |
| 虚拟时间（全程） | 5270 s | 4243 s | **4002 s** |
| 行程 | 20 508 m | 16 818 m | **14 319 m** |
| 检测次数 | 186 | 137 | 177 |

> 题目说"定位清除总时间包含机器狗移动时间、频道切换时间、检测时间、光学精确定位
> 时间及清除时间等"——搜索、绕行、空转都是移动+检测，**都属于其中**，所以
> "全程 ÷ 清除数"才是站得住的口径（`tour` 最好）。只有在"清完即停表"的口径下
> `field` 才更低，代价是它清完后还空转 1473 s（28%），且 30 局里漏 2 局源。

另外三批独立种子（各 30 局）同样 **30/30 全清**；四种情景（常规随机 / 外圈环带 /
中心密集 / 在线第 1 局真源）**全部 100% 清除**，且**平均定位清除时间与行程
在四种情景上都是三者最优**。两轮路径图复核修掉的缺陷
（"绕行太远就不清"、"为一条方位专门跑一趟"、"听到了却等下一站再折回"、
"进入环带的位置固定"等）连同最终配置的选定见 **`review/p3-tour-design.md`**。

```bash
python src/p3_run.py --mode mock --cases 30 --policy tour    # 统一航路（对照基线）
python src/p3_tour.py --selfcheck                            # T1–T6 自检
python src/p3_paths.py --policy tour --out-suffix _tour      # 路径图
python src/p3_compare_all.py --cases 30                      # 三套策略跨情景对照
python src/p3_route_bound.py --cases 30                      # 结构下界 L1/L2/L3
```

### 问题 3 的第四套策略：沿途定位与动态覆盖（`--policy adaptive`）

新方案用一个共享短基线开始定位，将单方位目标直接纳入清除航路，边逼近边补测；
以当前覆盖缺口动态选择临时测站，取消固定八点环带与角向游标。
新增按频道的连续靶区覆盖证书，只用实际 `no_signal` 测点排除单元。

冻结参数后的相同场景逐局对照（总虚拟时间，包含收尾排除与 `/exit`）：

| 验证场景 | 局数 | 原 tour | 新 adaptive | 节省 |
|---|---:|---:|---:|---:|
| 随机分布 | 50 | 4121 s | 3327 s | **19.3%** |
| 外圈分布 | 20 | 4336 s | 3464 s | **20.1%** |
| 非平滑固定位置误差 | 20 | 4144 s | 3434 s | **17.1%** |
| 接收半径全部为 1000 m | 20 | 4377 s | 3662 s | **16.3%** |

全部七类验证共 **160 局、2002 个源，新策略全部清除且通过连续覆盖判据**。
中心聚集场景总时间增加 2.7%，但最后清除时刻由 1393 s 提前到 673 s；
两个历史坐标重建布局的总时间改善分别为 8.7% 和 19.5%。这些均为离线测试，
没有消耗在线测试次数。详细方案、口径、逐局数据与图见
[p3-adaptive-design.md](review/p3-adaptive-design.md)。`adaptive` 已被下一节的 `joint` 取代，
现作为可选对照保留。

> **独立复核（2026-09-12，`review/P3-adaptive-verification.md`）**：上表是**冻结验证集**
> （`777000+37i`）的数字。换一套从未用于调参的种子 `3141592+91i` 做样本外复核后，
> **随机分布集只省 14.9%**（区间 12.9%–16.8%），外圈分布 19.3%、接收半径 1000 m 18.6%，
> **中心聚集 ±0%（既不变差也不变好，"慢 2.7%"是噪声）**。110 局 1409 个源仍全部清除。
> 同时用模拟器原始动作台账重算计时（偏差 0.00e+00 s）、用真值探针确认运行期没读真值、
> 用 5 m 细网格独立验证完工声明（最坏未覆盖点距 980 m < 1000 m，无空洞）。
> 所以诚实的说法是：**随机场景约省一成半、外圈/弱接收场景约省两成**，不是稳定 20%。
> 路径图：`out/p3_verify/paths_pair.png`（7 个案例同场景并排）、`time_split.png`。

从项目根目录运行新策略与验证：

```powershell
python B_locator/Q3/src/p3_run.py --mode mock --policy adaptive --cases 30 --out B_locator/Q3/out/p3_adaptive
python B_locator/Q3/src/p3_adaptive_checks.py
python B_locator/Q3/src/p3_adaptive_bench.py --out B_locator/Q3/out/p3_adaptive_validation_v2
python B_locator/Q3/src/p3_verify_paths.py --cases 3 --coverage-audit 20 --out B_locator/Q3/out/p3_verify
```

### 问题 3 的进一步优化：联合航路与检测调度（`--policy joint`，**当前默认**）

> **默认策略已由 `tour` 改为 `joint`**（`src/p3_run.py` 的 `DEFAULT_POLICY`）：
> 不写 `--policy` 时跑的就是这一套。`tour` / `adaptive` 保留作对照。

这一版先清除目标，再在实际终点扫描；每轮比较多条覆盖航路，删去冗余站，
在保持覆盖职责的前提下将测站移近清除航路，并减少低收益的顺手补测。
近邻补站不再因小于 60 m 而被跳过，两三个目标也按最短顺序处理。

使用新种子 `918000+53i`，三种策略在相同场景逐局配对，指标为完整退出时间：

| 场景 | 局数 | 原始 tour | 上一版 adaptive | 新 joint | 比上一版再省 |
|---|---:|---:|---:|---:|---:|
| 随机分布 | 50 | 4032 s | 3317 s | **3090 s** | **6.9%** |
| 外圈分布 | 20 | 4450 s | 3510 s | **3206 s** | **8.7%** |
| 中心聚集 | 10 | 2517 s | 2563 s | **2235 s** | **12.8%** |
| 非平滑误差 | 20 | 4053 s | 3291 s | **3108 s** | **5.6%** |
| 接收半径 1000 m | 20 | 4341 s | 3505 s | **3309 s** | **5.6%** |

加上两个重建布局，共 **140 局、1749 个源全部清除，每局通过连续覆盖证书**。
随机集 48/50 局快于上一版，检测次数从 157.54 降到 128.04。
**平均完整时间仍未达到 2800 s**；本轮的主要额外收益在减少检测。
12 项专项检查通过，保存的 63 份原始动作轨迹独立复算计时偏差为 0。
完整说明与逐局结果见 [p3-joint-design.md](review/p3-joint-design.md)。

同一场景下三套策略并排的路径图（颜色深浅 = 时间早晚，含逐案例指标与时间去向）：

```powershell
python B_locator/Q3/src/p3_verify_paths.py --cases 3 --out B_locator/Q3/out/p3_verify
```

产物：`out/p3_verify/paths_pair.png`（7 案例 × 3 策略）、`paths_overview.png`、`time_split.png`、
`verify_table.json`（逐条台账重算偏差）。

从项目根目录运行（`--policy` 已是 `joint`，可省略）：

```powershell
python B_locator/Q3/src/p3_run.py --mode mock --cases 30 --out B_locator/Q3/out/p3_joint
python B_locator/Q3/src/p3_joint_checks.py
python B_locator/Q3/src/p3_joint_bench.py
python B_locator/Q3/src/p3_run.py --mode live --robot-id <参赛队号>     # 默认即 joint
```
> 📌 **merge 备注（口径适用范围）**：下面这组对照实验跑在**旧口径**下 —— 当时的 `MockArena`
> 把**现实**预算 1200 s 误当成了**虚拟时钟**上限（见本节开头的口径修正）。该口径会大幅放大
> "预算不足"的影响，所以"全部清除物理上不可能"只属于 1200 s 口径：改用与真实模拟器一致的
> 360000 s 后，同一批 30 局的清除率是 **0.995**（上表）。四组开关保留在 `P3Config` 里、**默认全关**。
>
> 上界分析：1200 s、5 m/s 只能在靶区内跑约 4.2 km，而 12 个随机源的**最优航路**本身就约 7.7 km
> ⇒ 在 1200 s 口径下"全部清除"不可能；瓶颈是**信息获取成本**（发现 + 第二次定位测量 + 覆盖式试探），
> 而不是决策算法本身。详见 `review/P3-design.md` §5.2。
>
> **两个"看起来对"的改进已被实测否决**（60 局配对对照，见 `review/P3-design.md` §5.3）：
> ① "剩余时间不够时放弃扫描、直奔清除"（扫描闸门 / 扫描时间上限）——**持平到 −1.1pt**；
> ② "只走可达的近点"（近点子集 / 贪心 + 剩余预算可达 / 剩余预算裁剪）——**−1.4 到 −2.0pt**。
> 原因是代价估计 `estimate_clear_cost` 已经准确到略悲观（实际/估计中位数 0.87），
> 按它做可达性预判只会提前砍掉本来能清的目标。这四组开关保留在 `P3Config` 里、**默认全关**。
>
> 相对的，**真正的天花板**是：对本局实际曾定位的目标做最优航路，1200 s 内平均可清 **6.48 个**，
> 策略实际清 **5.10 个**（= 上界的 **78.7%**）⇒ 仅靠改进"已定位目标的排序与取舍"还有
> 约 **+1.4 个源/局（≈ +11pt）** 的空间。详见 §5.4。（以上 6.48/5.10 也是 1200 s 口径。）

**运行**

```powershell
python src/p3_expect_field.py --build-family      # 一次性：生成基准图族（约 13 s）
python src/p3_selftest.py                         # 40 项自检（决策场/接口层/在线策略/扫圈/统一航路）
python src/p3_run.py --mode mock --cases 30 --figure --out out/p3   # 默认策略 = joint
python src/p3_ablation.py --cases 30              # 参数灵敏度 + 消融表
python src/p3_run.py --mode live --robot-id <参赛队号>   # 真实模拟器（默认 joint；需先登录并开始测试）
```

> `--policy` 默认值现为 **`joint`**（第四轮优化）。要与旧结果对齐或做对照，
> 显式写 `--policy tour` / `--policy adaptive`。

产物：`out/p3/mock_benchmark.{json,csv}`、`out/p3/ablation.{json,csv}`、
`out/p3/case_<seed>/{summary.json, actions.jsonl, robot_events.json, p3_case.png}`、
`out/p3/p3_field.png`（决策场三联图）、`out/p3_paths_tour/`（统一航路路径图）、
`review/out_p3_selftest.txt`。
