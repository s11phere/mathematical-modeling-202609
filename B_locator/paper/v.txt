无线电干扰源的快速自动定位与清除模型
摘要
【待填充：开头总述，一句话交代背景与要解决的问题，一句话说明本文总体上采
用什么模型与算法体系，并串起四问“单次定位 → 检测点布置 → 多源清除 → 定向源
辨识”。】
针对问题一，基于示向度误差有界的特点，本文建立了有界区间角楔交会模型，把
每个检测点的示向度按误差界张成角楔，干扰源必在各角楔交会的公共部分中。由于形
成的定位区域为凸多边形，直径归结为顶点集上的最远点对。使用程序模拟构造的五组
算例直径在 10.59 ∼ 25.64 m 之间，而以该直径为直径的圆并不总能覆盖定位区域，五
组算例中有两组失败，越界顶点到圆心距离超过半径约 10.6%。
针对问题二，只掌握一个检测点处的示向度、源点距离未知，无法先定位再布点，
故建立以期望定位区域直径为准则的布点模型：由两探测束的交会几何写出定位区域直
径，并对源点位置与第二次测向的误差求取期望，得到只依赖第二个检测点相对第一个
检测点位置 (a, b) 的二维价值函数 E[D](a, b)，使用理论方法求解与数值模拟方法求解
获得结果吻合。我们选择一个合理阈值 60m，则第二检测点和候选区域即为相对位置满
足E[D] < 60m 的区域C = {(a, b) E[D](a, b) < 60}，具体区域形状见下正文。
针对问题三，【待填充：多源定位与清除策略及实测指标。】
针对问题四，【待填充：含定向源的检测与清除策略及实测结果。】
【待填充：结尾总评，四个模型的优点、缺点各一两条，并给出可继续改进的方向。
】
关键词： 示向度交会

直径圆覆盖

1

一、 问题背景与重述
1 . 1 问题背景

随着无线电技术的普及和广泛应用，频谱管理中的干扰源定位与清除问题日益突
出。干扰不仅影响正常通信，还可能危及公共安全。传统清除方式通常受环境遮挡、低
功率发射等影响，精准定位耗时长，且危险环境下人工作业风险高，因此研发机器狗自
动定位清除系统代替人工执行任务具有现实需求。但任务中通常面临着目标区域范围
较大，干扰源数量、频道、位置、有效接收半径及类型未知，测向存在误差，移动、检
测、切换频道和清除过程存在耗时，定向干扰源的发射方向无法辨识等困难。如何在信
息不完全且时间受限的条件下高效完成任务，是该自动定位清除系统亟待解决的问题。
1 . 2 问题重述

考虑到上述实际情况中面临的困难，本文需依次解决以下四个问题：
1. 已知若干检测点的位置及干扰源关于这些检测点的示向度，给出用交会定位法计算
可能位置区域（多边形）直径的算法，并回答以该直径为直径的圆能否覆盖此区域。
2. 已知在某一个检测点处测得的示向度，给出第二个检测点的选择策略，并指出该点
可取在什么范围（候选区域）之内。
3. 区域内存在多个干扰源，它们占用互不相同的频道且总个数未知，机器狗从给定起
点出发自动定位并逐个清除。要求给出策略与算法，经模拟器演练检验后给出清除
比例与平均定位清除时间，并据以完成正式测试。
4. 区域内除全向干扰源外还混有定向干扰源，其个数与发射方向均未知，其余条件与
问题三相同。要求给出相应的检测与清除策略及算法。

二、 问题分析
四个子问题层层递进。问题一是单次交会定位的几何基础；问题二则将该几何模型
进一步用作检测点位置的选择依据，形成单源定位策略；问题三在严格的时间预算下，
把单源定位能力嵌入“测向、收敛、清除”的在线决策；问题四基于问题三模型的基础
之上，再额外引入定向源方向诊断对决策的影响。
2 . 1 问题一的分析

本问不宜假设误差分布，而应利用题目给出的误差界，采用最坏情形模型：每个检
测点的示向度对应一个由“测量方向 ±1◦ ”张成的角楔。多个检测点的角楔求交，即得
干扰源的可能定位区域。由于角楔是凸集，其交仍为凸集（有界时即为凸多边形），因
此区域直径可转化为凸多边形顶点间的最远点对问题。求解时，需要先判断交会区域是
否有界，再检验以该直径为直径的圆能否覆盖定位区域。整体求解思路可概括为：由误
差界构造单点角楔，通过多检测点角楔交会确定凸多边形定位区域，进而将区域直径计
算归结为顶点间的最远点对问题；同时辅以有界性判定与圆覆盖检验。

2

2 . 2 问题二的分析

问题的关键在于第二个检测点无法由单次观测唯一确定：只掌握 S1 处的示向度，源
点的距离信息完全缺失，故既不能先定位再布点，也不存在一个位置在每次试验中都最
好。本文改以“布点后的定位效果之期望”反向衡量布点位置，沿用问题一中“定位区
域直径”这一判据，取其对第二次检测点示向度 θ2 的测量不确定性与源点位置的不确
定性的期望为关键判据，既不依赖某一次定位的运气，也无需先知道源点的距离。又因
直径的期望只由两次探测的交会几何决定，它只依赖第二个检测点相对第一个检测点的
位置，与绝对坐标无关。于是本问归结为以第二检测点对第一检测点的相对位置为决策
变量、以 E[D](a, b) 为价值函数的二维寻优问题，其等值线内部有界域即第二个检测点
的候选区域。
2 . 3 问题三的分析

【待填充：给出策略与算法，并补充模拟与实测指标。】
2 . 4 问题四的分析

【待填充：给出定向源的检测与清除策略、诊断规则及实测结果。】

三、 模型假设
为便于建模求解，在合理范围内作如下假设：
1. 示向度误差有界，|∆θi | ≤ ε（本题 ε = 1◦ ）
，且同一检测点的误差在观测期间固定不
变，只有更换地点才呈现统计规律；故采用有界区间模型（最坏情形）
，所得区域保
证包含真值。
2. 检测点坐标与示向度读数精确已知；
示向度以 x 轴正向逆时针为正，
取值于 [0◦ , 360◦ )。
3. 干扰源位置在观测与清除期间固定不变，各源频道互不相同且不随时间变化。
4. 测向机与干扰源位于同一平面，区域内无障碍遮挡。
5. 全向源的覆盖角度为 360◦ ，定向源为定向方向两侧各 90◦ （含边界），覆盖范围内信
号均匀辐射、场强仅随距离衰减。
6. 机器狗沿直线以 5 m/s 匀速行进，忽略转向与加减速耗时；移动中不可检测，单次检
测 5 s，任意两频道切换 1 s。
7. 与干扰源距离不超过 20 m 时，光学定位（3 s）与激光清除（2 s）必定成功且与覆盖
角度无关；距离不超过 5 m 且处于覆盖角度内时无法获得示向度，可直接定位并清
除。

四、 符号说明
本文使用的主要符号见表 1，未列出的符号在首次出现处说明。

3

表1

主要符号说明

符号

含义

单位

Si = (xi , yi ), θi
T = (xT , yT )
∆(α, β)
ε
R, V
D
M
A, B
S1 , S2 = (a, b)
θ2
φ
xmax
E[D](a, b)

第 i 个检测点的坐标与示向度
m, ◦
干扰源（待定位目标）的真实位置
m
◦
◦
◦
两方位角之差，归一到 (−180 , 180 ]
◦
示向度误差界（本题 1◦ ）
定位区域及其顶点集
无
定位区域直径
m
直径圆圆心（直径中点）
m
直径端点
m
第一、第二个检测点及其相对位置
m
◦
第二个检测点处的示向度
◦
候选区域极角（自 S1 的示向度方向量起）
干扰源最大可被有效接收距离
m
定位区域直径的期望（第二个检测点的价值函数） m

五、 模型建立与求解
5 . 1 问题一：示向度交会定位模型

5 . 1 . 1 模型建立
，
设第 i 个检测点为 Si = (xi , yi )，
测得的示向度为 θ（x
轴正向逆时针，
取值 [0◦ , 360◦ )）
i
误差界为 ε = 1◦ 。干扰源则落在以 Si 为顶点、关于示向度方向对称张开 2ε 的角楔之内

（为避免方位角相减在跨 0◦ 时出错，记两角之差 ∆(α, β) = (α − β + 180◦ ) mod 360◦ −
180◦ ）：


(1)
Wi = T : ∆ β(Si , T ), θi ≤ ε ,
其中 β(S, T ) 为自检测点 S 指向干扰源 T 的方位角。m 次测向给出的定位区域即各角楔
之交，如图 1 所示（以 m = 3 为例）。每个角楔可表示为两个闭半平面之交；各角楔对
T
干扰源的约束同时成立，故定位区域为 R = m
i=1 Wi ，它是 2m 个半平面之交，为凸多
边形区域，可能有界也可能无界。区域有界时其顶点数不超过 2m，且每个顶点必是某
两条边界直线的交点。记该区域的顶点集为 V 。
若各示向度方向过于接近，R 沿公共方向无限延伸，定位区域的直径无定义，故
须先判定有界性。由于无界是无穷远处的性质，判据只涉及方向：角楔是顶点在 Si 的
锥，若某方向同时落在各角楔张开的方向区间内，则区域内任一点沿该方向平移任意
远都不会离开区域，区域因而无界；反之，若区域无界，取其中趋于无穷的一列点单
位化，由单位圆的紧性可取收敛子列，而自各 Si 指向该点列的方位角都趋于该极限方
向，角楔又是闭集，故极限方向必落在每个区间内。于是 R 无界当且仅当各方位角区
间 Θi = [θi − ε, θi + ε]（跨 0◦ 按圆周区间处理）存在公共交集，当 m = 2 时即两次测向
的方向夹角不超过 2ε。该判据只需 O(m) 次角度比较，且不依赖检测点坐标。
对已判定为有界的区域，凸多边形上任意两点的最大距离必由两个顶点达到。故求
4

图1

多检测点示向度交会得到的定位区域；右下角的局部放大图中标出交点 Vi 和直径
中点 M

直径不必在连续区域上作最优化，只需在顶点集 V 上比较点对距离：
D = max ∥P − Q∥ = max ∥P − Q∥.
P,Q∈R

P,Q∈V

(2)

设使式 (2) 取到最大值的两个顶点为 A, B，则直径圆圆心为 M = (A + B)/2、半径为
r = D/2。
覆盖与否由圆盘的凸性直接判定：R ⊆ 该直径圆当且仅当其全部顶点含于圆内，即
max ∥P − M ∥ ≤ D/2.
P ∈V

(3)

条件不满足时，必有顶点 P 使 ∠AP B < 90◦ 而落在圆外。注意该直径圆的圆心固定为
M ，与圆心可自由选取的最小包围圆不同，故式 (3) 需逐个顶点检验。注意到最远点对
可能不唯一，但覆盖判定与取法无关：若某一对最远点 A, B 确定的圆已覆盖 R，则 R
含于半径 D/2 的圆内，而 R 内任意两点距离不超过 D，故任何其他最远点对都必对应
于该圆上的一条直径，其确定的圆与原圆重合；若某一对不能覆盖，则任何一对也都不
能。因此下文任取一对最远点构造直径圆即可。
5 . 1 . 2 模型求解算法
算法输入为 m 组“检测点坐标与示向度”及误差界 ε，输出为顶点集 V 、直径 D、
直径端点 A, B、圆心 M 及覆盖判定，流程如图 2，分四步。
第一步（有界性预判）
：对 m 个方位角区间 [θi − ε, θi + ε] 求交。若交集非空，则定
位区域沿该方向无限延伸，直径无定义，算法返回状态 unbounded，并提示须更换检测
点重测。
5

图2

问题一求解算法流程

第二步（区域构造）
：区域的每个顶点都是两条边界直线的交点，故算法枚举每个角
楔两条边界射线所在直线（共 2m 条）的两两交点，再用全部 2m 个半平面判据逐一筛
选；同一角楔两条边界线的交点即检测点 Si ，它同样作为候选点保留。随后对候选点集
求凸包（包含全部候选点的最小凸多边形，用 Andrew 单调链实现）
，剔除重复点与近似
共线点，得到逆时针排列的顶点表 V 。若候选点集为空，说明各角楔无公共交集（测量
数据自相矛盾）
，算法返回状态 empty （无可行定位区域）并终止该例计算；若 |V | < 3，
说明交集退化为点或线段，算法返回状态 degenerate（退化）并终止。
第三步（求直径）
：按式 (2) 枚举 V 中的顶点对取最大距离，对应两点记为 A, B，圆
心取 M = (A + B)/2；本题 m ≤ 20，直接枚举即可（至多 O(m2 ) 次距离计算）。为交
叉验证，本文还采用旋转卡壳算法（凸多边形直径的线性时间算法）复算 D，两法结果
一致。
第四步（覆盖判定）：按式 (3) 计算 maxP ∈V ∥P − M ∥，判定条件为
max ∥P − M ∥ ≤ D/2 + τ,
P ∈V

(4)

其中绝对容差 τ = 10−9 m 用于容忍相切构型的浮点误差，避免相切构型被误判为不覆
盖；判定不覆盖时，输出取到该最大值的顶点作为越界位置。
为保证结果不依赖具体实现，程序另有一套独立等价的区域构造算法：自一个大正
方形出发，用 Sutherland–Hodgman 算法逐个半平面裁剪多边形。两者给出的顶点集逐
点一致（最大偏差不超过 10−9 m），状态名与原因说明见附录 B 的 analyse()。
5 . 1 . 3 求解结果与分析
使用程序构造五组良态算例（检测点分布在干扰源四周，方位张角 180◦ ∼ 249◦ 、最
小方位间隔不小于 25◦ 、与干扰源距离 160 ∼ 1300 m）
，结果见表 2；五组算例的定位区
域直径均在 10.59 ∼ 25.64 m 之间，即两次以上交会已能把候选范围从测向距离的百米
量级收缩到数十米以内。表中同时给出覆盖判定，其两种结果的对照见图 3：左图（算
例 A1 ）的定位区域两个顶点都落在直径圆上，最远顶点到圆心 M 的距离恰为 D/2（比
值 1.0000），此时直径圆就是该区域的最小包围圆；右图（算例 A2 ）中，橙色虚线标出
的顶点 P 落在圆外，|P M | = 10.29 m 比 D/2 = 9.31 m 大 0.98 m（比值 1.1055），直径
圆覆盖判定为“否”。
6

表2

问题一算例的计算结果

算例 m 顶点数 D (m) D/2 (m) max ∥P − M ∥ (m)

比值

覆盖

A1
A2
A3
A4
A5

1.0000
1.1055
1.0722
1.0000
1.0000

是
否
否
是
是

6
5
4
5
6

6
5
5
4
5

17.64
18.61
25.64
21.12
10.59

8.82
9.31
12.82
10.56
5.30

8.82
10.29
13.75
10.56
5.30

可见五组算例中有两组（A2 、A3 ）的直径圆无法覆盖定位区域，越界顶点到 M 的
距离分别比 D/2 大 0.98 m 与 0.93 m，超出 10.55% 与 7.22%；这两组算例的区域均为斜
五边形，由于 A2 中两个顶点距离较近，故可以近似为四边形，而越界顶点所对直径形
成的张角小于 90◦ （图 3右图中圈出的顶点张角为 84.2◦ ），因而落在圆外。

图3
表3

直径圆覆盖判定的两种结果：(a) 覆盖成立；(b) 覆盖不成立

四组随机扫描的覆盖失败统计（固定种子，理想/实际构型逐例配对）

构型 检测点数 试验数 有界数 失败数

失败率

最坏比值 需放大约

理想
理想
实际
实际

1.16%
3.25%
1.23%
28.60%

1.0336
1.0304
1.0346
1.6876

2
3∼6
2
3∼6

20000
4000
20000
4000

20000
3472
19996
3472

231
113
246
993

3.4%
3.0%
3.5%
68.8%

注：理想构型指各示向度精确指向源点；实际构型指在同一批构型上叠加与算例生成同分布的 ±ε 测向误差；
“需放大约”为最坏比值减 1。

为说明这种失败并非偶然，本文以固定随机种子作四组扫描（表 3）
。叠加测向误差
后，3 ∼ 6 个检测点的覆盖失败率由 3.25% 升至 28.60%、最坏时直径圆需放大约 69%，
且失败率随检测点数递增；可见只要各次测向的误差方向不一致，定位区域便会偏离以
源点为公共交点的对称形状，直径圆随之失效，表 2 中 A2 、A3 的失败即属于这一情形。
7

本节数值的数据清单与复现命令见附录 A。
5 . 2 问题二：第二检测点的选择模型

5 . 2 . 1 模型建立
记号与概率描述
S1 置于原点，其示向度方向取为 x 轴正方向；S2 置于 (a, b)。已知 S1 的示向度后，
源点 T 可能位置 (xT , yT ) 服从一个条件概率分布

(5)
T ∼ PT T ,
而给定源点位置与 S2 的位置，θ2 的读数由于误差亦不能确定，近似服从

θ2 ∼ Pθ θ2 | T, S2 .

(6)

位形参数 (θ2 , a, b) 一经确定，定位区域直径便由几何关系唯一确定给出：
D = D(θ2 , a, b).

(7)

于是 D 服从由几何参量 (a, b) 控制的概率分布，第二个检测点的选择问题即化为求如下
二维价值函数：
ZZ
Z


2
E[D](a, b) =
d ⃗rT PT T | S1
dθ2 D(θ2 , a, b) Pθ θ2 | T, S2 ,
(8)
E[D](a, b) 越小，在该点放置第二个检测点所得定位平均效果越好。这即为问题二求解
的关键衡量指标。
5 . 2 . 2 理论求解
(a) 理论推导
式 (8) 含内外两次共计三重概率积分，直接使用不便，故作以下五条近似：
1. S2 距 S1 的距离 R 不太小。该近似的合理性并非天然成立：S2 距 S1 过近时两视线
的交角趋于零，所得定位区域必然很大，这是我们不希望的，故 R 较小的区域本来
就不在选择范围之内，而本问关心的待选区域附近该近似较好。
2. 忽略源点纵坐标，认为其恰好落在 x 轴上，其位置只由一个参数 xT 描述。当 R 不
小时源点纵坐标 yT 是小量，将分布对 yT 作展开：
Pθ (θ2 | xT , yT , a, b) ∼ Pθ (θ2 | xT , 0, a, b) +

d
Pθ yT =0 yT + o(yT )
dyT

(9)

而在第一个概率积分中区间关于 yT = 0 对称，一阶项相互抵消，故 yT 对 E[D] 的影
响是高阶小量。
3. 考虑 yT 小量近似后所得零阶项 Pθ (θ2 | xT , 0, a, b) 在扇形区间内积分，可认为先验权
重满足 PT (xT | S1 ) ∝ xT ，归一化后得

2xT
PT x T | S1 = 2 ,
xmax

xT ∈ [0, xmax ],

其中 xmax 为干扰源的最大可被有效接收距离，在本问中可直接取为 1500m。

8

(10)

4. 与第 2 条同理，由于检测点二实际测得方向角在理论值 θ2,ideal 左右 1° 区间内均匀分
布，偏离值为小量认为其分布近似 θ2 ∼ δ(θ2 − θ2,ideal )，即等效直接认为第二次探测
所得方位正对源点。
5. 在计算第二检测点的定位区域直径期望时忽略探测半径的影响，而将第二检测点的
定位区域角楔延伸到足够长进行计算。这条近似在第二检测点距离第一检测点定位
区域不太远时成立。因为此时整个第一检测点的定位区域完全包含在第二检测点的
定位区域内，第二检测点的探测半径对定位区域直径的影响可以忽略。
五条近似把价值函数精简为单个一维积分：
Z xmax

(11)
E[D](a, b) =
dxT PT xT | S1 D(xT , a, b).
0

(b) 定位区域直径的解析推导
近似取定 θ2 正对源点后，两条探测视线在源点处的交角 α 满足
p
|b|
sin α =
,
r2T = (xT − a)2 + b2 ,
(12)
r2T
图 4 为一个特定情况下的两个检测点示意图，并取源点 T 、第二检测点 S2 ；图中
蓝、黄两个区域分别为两次探测各自的定位区域，∠S1 T S2 = α 为两视线在源点处的交
角，右上角为两区域相交所得的定位区域 ABCD 的局部放大图。所得定位区域为局部
图中的红色近似平行四边形 ABCD：该区域的直径即对角线 |BD|。

图4

两探测束交会产生定位区域示意图

本文探测束张角极小（∆θ = 2ε = 2◦ ≈ 0.0349 rad ≪ 1）
，故在源点处两次探测束的
横向宽度分别为
p
w1 = xT ∆θ,
w2 = r2T ∆θ = (xT − a)2 + b2 ∆θ.
(13)
由式 (12) 及 (xT − a)/r2T 的符号，交角还满足
xT − a
xT − a
cos α =
.
=p
r2T
(xT − a)2 + b2
9

(14)

其对角线长度由两组对边的宽度与夹角 α 完全确定：
p
w12 + w22 + 2w1 w2 cos α
D(xT , a, b) =
.
sin α

(15)

该式有直接的几何解释：
下标 1、
2 的两项分别对应两次探测各自的束宽，
交叉项 2w1 w2 cos α
则来自两组对边之间的夹角。
把式 (13)、(12) 与 (14) 代入式 (15)，即可消去 w1 , w2 , α，将直径完全显式化为
2
a, b, xT 的函数。分子先提出公因子 r2T ∆θ，被开方式化为 x2T + r2T
+ 2xT (xT − a) =
2
2
2
4xT − 4axT + a + b ，于是
q
∆θ r2T
2 + 2x (x − a)
D(xT , a, b) =
x2T + r2T
T
T
|b|
(16)
p
∆θ p
2
2
2
2
(xT − a) + b
(2xT − a) + b ,
=
|b|
其中第一个根式即式 (12) 中的 r2T = |S2 T |，第二个根式是源点到点 (a/2, −b/2) 之距离
的两倍。定位区域直径由这两个几何量的乘积与 |b| 之比给出。
式 (16) 的合理性可由两个极限情形检验：当第二个检测点正落在第一个视线方向
附近（a → xT , b → 0）时交角趋于 90◦ ，此时 D → ∆θ xT = w1 ，恰为定位区域沿垂向
的宽度；当 b → 0 且 xT ̸= a 时两视线共线、sin α → 0，D → ∞，与式 (12) 的退化判据
一致。
代入 ∆θ = 2◦ = π/90 rad，得直径函数的解析表达式
p
π p
D(xT , a, b) =
(xT − a)2 + b2 (2xT − a)2 + b2 .
(17)
90 |b|
再代回式 (11) 并利用式 (10)，即得可直接用于数值积分与优化的目标函数
Z xmax q



π
xT
(xT − a)2 + b2 (2xT − a)2 + b2 dxT .
E[D](a, b) =
2
45 xmax |b| 0

(18)

引入无量纲变量 u = xT /xmax ∈ [0, 1] 与 â = a/xmax 、b̂ = b/xmax ，式 (18) 还可写成
Z
q


πxmax 1
(19)
E[D](a, b) =
u (u − â)2 + b̂2 (2u − â)2 + b̂2 du,
45 |b̂| 0
即 E[D] 只依赖 xmax 与无量纲位形 (â, b̂)，与绝对尺度无关。
(c) 价值函数的闭合解
价值函数的不定积分核为
Z
q


xT
(xT − a)2 + b2 (2xT − a)2 + b2 dxT .
作平移 t = xT − a/2（即 xT = t + a/2）
，两个二次因子化为关于 t 的偶函数


a 2
+ b2 ,
(2xT − a)2 + b2 = 4t2 + b2 ,
(xT − a)2 + b2 = t −
2

(20)

(21)

即根号内是 t 的四次多项式（偶四次）。因此式 (20) 的原函数属于椭圆积分类型，不能
由初等函数显示表示。
我们使用 Mathematica 软件对理论公式 (18)进行了计算绘图，计算结果见 5.2.4 节。
10

5 . 2 . 3 数值方法求解
为了与由理论近似解析式得到的 E[D](a, b) 交叉验证，我们使用 Python 程序对该分
布作了数值求解。算法输入为已知示向度方向（用于建立坐标系）
、有效接收半径 xmax
与误差界 ε；输出为定位区域直径期望的分布 E[D](a, b) 最优相对位形 (a∗ , b∗ )、最小期
望直径 E[D]min 与候选区域，分四步。
第一步建立标准坐标系：S1 置于原点，其示向度方向取为 x 轴正向，S2 = (a, b)；
于是几何量 |b|、r2T 、α 都可用 a, b, xT 写出。
第二步 D 的精确计算：对给定 (a, b, xT )，作 S1 的两条边界射线（与 x 轴成 ±ε）与
S2 的两条边界射线（绕“S2 → T ”方向成 ±ε）
。四条直线两两求交得 6 个候选点，再用
角楔定义筛出真正落在两个楔形内的顶点（良态下恰为 4 个，即四边形 ABCD），取
D(xT , a, b) =

max

P,Q∈{A,B,C,D}

|P Q|;

(22)

两条边界线平行（两视线共线）时交会区域无界，该源位样本直接舍弃。这一步用的是
四边形的严格几何，与前文解析式的平行四边形近似无关。
第三步求期望。源位采样取在 x 轴正半轴上，上限为有效接收半径上界，在 [0, xmax ]
(k)
上取 n 个分点，以 wk ∝ xT （即式 (10)）加权。要注意只有“第二个检测点也能收到
该频道信号”的源位样本才是可用的：对每个样本还须满足 |S2 Gk | ≤ xmax 。把不满足
该条件或无界的样本剔除后，在剩余样本上重新归一，得

P
(k)
,
a,
b
w
D
x
k
k∈K(a,b)
T
(k)
P
,
w k ∝ xT ,
(23)
E[D](a, b) =
w
k∈K(a,b) k
其中 K(a, b) 为可用样本集合。式 (23) 是条件期望 E[D | 两次探测均有效]，而非对所有
源位的无条件平均；这一区别在 S2 远离 S1 时不可忽略，故本文一律采用式 (23)。
第四步寻优：网格取以 xmax 为半径的靶区圆域、步长 30 m，剔除 |y| ≤ 60 m 的近
轴退化带（该带内两视线近简并、交会区域被拉成极窄长条甚至无界），其余格点逐点
代入式 (23)，取最小值点即 (a∗ , b∗ )。为避免近轴期望值发散区域干扰绘图颜色映射，程
序中把 E[D] 超过全体可用格点中位数 10 倍的格点标注为不可用带，不参与寻优。
5 . 2 . 4 求解结果与分析
我们取 xmax = 1500 m、ε = 1◦ ，通过上述两套相互独立的计算方法分别得到结果，
并在最后据此确定第二检测点的待选区域。
(a) 理论解计算结果
按照 5.2.2 节分析，我们使用 Mathematica 软件进行计算，将式 (19) 的闭式被积函
数，把式 (17) 的直径代入对源位的加权积分，在 15 m 步长的矩形网格上直接作高密度
求积，计算网格覆盖 a ∈ [−500, 1795] m、b ∈ [−1800, 1800] m。图 5(a) 给出 E[D](a, b)
在所在区域内的函数图像。其中 a < −500 区域不在计算处理范围内，绘制为白色。而
接近 x 轴部分 E[D] 发散，超出绘图范围与机器精度限制，亦绘制为白色。
(b) 数值模拟计算结果
我们按 5.2.3 节的算法，使用 Python 程序，对每个格点由两个角楔的严格几何求出
交会四边形直径，再对源位加权求积绘制了 E[D](a, b) 图像；网格为以 xmax 为半径的
11

靶区圆域、步长 30 m。由于在数值模拟中并未采取理论推导时的近似，故绘图区域限
制更大。|S2 S1 | > xmax 或 |S2 G| > xmax 导致两次探测不可能同时有效接收的不可行区，
与两视线近似共线、E[D] 超出色标上限而被截断的区域均绘制为白色
图 5(b) 给出同一价值函数由数值模拟得到的分布。与 (a) 相比，两者的整体形态、
极小值位置与色带走向完全一致，极小值为 E[D]min = 57.34 m，位于 (1080, ±660) m。
(c) 结果交叉对比
图 5 中的两幅图绘制在参数与颜色映射完全相同的两个坐标系中。直观可见在二者
定义域重叠范围内，计算结果的整体形态吻合度极高。

图5

期望定位区域直径的分布：理论闭式解与数值模拟的并排对比。(a) Mathematica
按解析式在 15 m 网格上求积；(b) Python 按交会四边形严格几何在 30 m 网格上
求解。两图的极小值位置与色带走向一致

为进一步定量分析，我们把 MMA 的 15 m 网格结果双线性插值到数值模拟的 30 m
格点上，对两者直接逐点比对。表 4 列出若干代表性位置的结果。在最优带（E[D] ≤ 60
m 区域）内 374 个可对比点上，两者的相对偏差中位数为 0.14%、最大不超过 0.28%；放
宽到 E[D] ≤ 100 m 的 3302 个点上，相对偏差中位数为 0.18%、最大不超过 0.51%。两
条完全独立的求解路线在关心的取值范围内完美吻合，说明 5.2.2 节的解析推导、5.2.3
节的四边形几何与其数值自洽自洽性。
在两种计算结果中观察 E[D] 图像性质，其取值从 57 m 一直延伸到色标上限 103 m
以上，且在极小值附近平缓而在 x 轴附近发散，故图中采用非线性色标：把 55 ∼ 100 m
一段拉开、100 m 以上压缩，提升问题更关心的低值区的区分度。图像关于 b = 0（S1 的
视线方向）严格上下对称，极小值成对出现，这是由体系对称性所决定的。E[D] ≤ 60m
的最优带为极小点周围的一小片区域，在此范围内靠近和远离检测点一的探测区间，都
会使得最终探测区间的直径期望增大。这是由于当检测点二距离检测点一的定位区域
很近时，观测角夹角正弦 sinα 平均值小，使得最终定位区域重叠极大，直径快速发散。
当检测点二距离检测点一的定位区域很远时，检测点二的定位区域在干扰源点处展宽已
经变得较大，使得定位区域直径增大。

12

表4

理论闭式解与数值模拟在代表性第二检测点位置的对比

a (m) b (m) 理论闭式解 E[D] (m) 数值模拟 E[D] (m) 相对偏差
1080
1050
1110
1200
1200

660
660
750
690
600

57.256
57.260
57.784
58.932
59.194

57.336
57.351
57.849
58.963
59.222

+0.14%
+0.16%
+0.11%
+0.05%
+0.05%

1200
900
1350
900
1410

900
900
750
1200
600

60.441
61.038
64.434
65.973
69.941

60.475
61.154
64.406
66.066
69.856

+0.06%
+0.19%
−0.04%
+0.14%
−0.12%

1500
600
300

900
600
900

72.758
78.228
94.310

72.684
78.499
94.561

−0.10%
+0.35%
+0.27%

(d) 待选区域确定
问题二要求给出的是第二检测点的可选范围。综合上文计算结果，本文选取 E[D] ≤
60 m 的区域作为第二检测点的待选区域：该阈值比极小值高约 5%，既保证定位效果
（期望直径接近销毁半径数量级）
，又给现场留出足够的位形余量。具体范围见图 6所示。
两套计算给出的同一区域的边界几乎重合。实际布点时建议取区域中心附近的 (a, b) ≈
(0.71 xmax , 0.43 xmax ) = (1065, 645) m，此时 E[D] 接近极小值，在期望意义下最利于确
认干扰源位置。
5 . 3 问题三：多源定位与清除策略

5 . 3 . 1 模型建立
【待填充：给出该问的模型与求解方法。】
5 . 3 . 2 求解与结果
【待填充：给出该问的求解结果与实测指标。】
5 . 4 问题四：含定向源时的定位与清除模型

5 . 4 . 1 模型建立
【待填充：给出该问的模型与求解方法。】
5 . 4 . 2 求解与结果
【待填充：给出该问的求解结果与分析。】

13

图6

E[D] ≤ 60 m 等值线围出的第二检测点待选区域

六、 模型评价与改进
6 . 1 灵敏度分析

【待填充：对模型中的关键参数作灵敏度分析。】
6 . 2 模型优点

【待填充：完成问题二至四后统一总结四个模型的优点。】
6 . 3 模型缺点

【待填充：同上，统一总结缺点。】
6 . 4 改进方向

【待填充：统一给出改进方向。】

参考文献
[1] 作者. 无线电测向与定位技术 [M]. 出版社, 年份.
[2] 作者. 论文题目 [J]. 期刊名, 年份, 卷 (期): 页码.
[3] 全国大学生数学建模竞赛组委会. 全国大学生数学建模竞赛论文格式规范（2026 年
修订稿）.

附录 A

支撑材料文件列表

本论文支撑材料所包含的文件如下（路径均位于 B_locator/ 目录下）
。问题一、问
题二的算例与全部数值结果均由清单中的脚本运行生成，问题一插图已定版，随材料一
并提供；数值计算仅依赖 NumPy，全过程离线。
14

1. Q1/PAPER-DATA.md：问题一全部数值的数据清单、来源字段与一键复现命令（正
文表 2、表 3 与摘要数值均可据此复核）。
2. Q1/p1_intersection.py：问题一求解库，含定位区域构造、直径与覆盖判定（附
录 B）。
3. code/main.py：问题三、四主程序，负责模拟器交互与搜索清除。
4. Q1/p1_experiments.py：算例生成、逐例求解与四组随机扫描（正文表 2、表 3
的数值来源）。
5. Q1/p1_selftest.py：自检脚本，含真值包含、凸性、直径双算法、两套区域实现
交叉校验与逐字节复现校验。
6. Q1/cases/p1_case01∼05.csv（含 .truth.json）
：五组算例与真值（固定种子生
成，真值仅自检用）。
7. Q1/p1_results.jsonl、Q1/p1_summary.json：逐例结果与汇总统计（含四组随
机扫描）。
8. figures/ 下的 3 张问题一插图（定版；Q1/ 下有同名副本，逐字节一致）：图 1、
图 2、图 3。
9. Q2/pysimulation/src/p2_grid_expectation.py：问题二数值模拟源程序，交会
四边形直径、加权期望、寻优与候选区域（附录 C）。
10. Q2/MMAcode/expected_d mathematica_ver.nb：理论闭式解的 Mathematica 程序，
导出 15 m 网格上的 E[D] 与最优点。
11. Q2/pysimulation/out/p2_grid/p2_grid_map.csv、p2_grid_report.json：数值
模拟逐点结果与汇总统计（本文数值来源）。
12. Q2/make_q2_paper_figures.py：问题二正文插图生成脚本（由上述两份原始数
据出图）。
13. figures/p2-mma-vs-pysim.png：理论闭式解与数值模拟的并排对比（图 5）。
14. figures/p2-contour60.png：E[D] ≤ 60 m 等值线围出的待选区域（图 6）。
15. figures/p2-wedge-geometry.png：两探测束交会与定位区域直径构造（图 4）。
16. Q2/Q2modeling.md 等：问题二建模手稿、参考热力图与 Mathematica 校验脚本。

附录 B

问题一主要源程序

问题一的完整求解源程序全文见下；定位区域按“顶点枚举 + 半平面筛选 + 凸包
规范化”构造，另有 Sutherland–Hodgman 裁剪的等价实现用于交叉校验。算例生成、四
组随机扫描与不变量自检所用的脚本及其用途见附录 A。
Listing 1: 问题一：定位区域、区域直径与直径圆覆盖判定（Python 3）
# -*- coding: utf-8 -*"""B 题问题一：示向度交会定位区域、区域直径与直径圆覆盖判定（纯求解库）。
约定：坐标单位 m，x 轴正东、y 轴正北；示向度 theta 为自 x 轴正向逆时针的方位角
（度，[0,360)）；示向度误差有界，|e| <= BEARING_ERR = 1 度。
定位区域 R = 各角楔 W_i 的交集，W_i 是以检测点 S_i 为顶点、张角 2*BEARING_ERR 的角楔

15

（两个半平面之交），故 R 是顶点数不超过 2m 的凸多边形；直径由顶点集上的最远点对给出，
覆盖判定比较 max|P-M| 与 D/2（M 为直径中点）。
入口：算例生成、逐例求解与四组扫描（理想构型与实际构型各两组）见 p1_experiments.py；
自检见 p1_selftest.py。
"""
from __future__ import annotations
import csv
import json
import math
import os
import numpy as np
BEARING_ERR = 1.0 # 示向度误差界（度）
ANG_TOL = 1e-9 # 角度判据容差（度）
DIST_TOL = 1e-6 # 半平面判据容差（米）
CLIP = 1e7 # 裁剪参考实现的外框半边长（米）
DIAM_BRUTE_LIMIT = 128 # 顶点数不超过该值时直径用暴力枚举

# -------------------------------------------------------------------------# 基本几何
# -------------------------------------------------------------------------def bearing(p, q):
"""p 处指向 q 的方位角（度，[0,360)）"""
return math.degrees(math.atan2(q[1] - p[1], q[0] - p[0])) % 360.0

def ang_diff(a, b):
"""a-b 归一化到 (-180,180]"""
return (a - b + 180.0) % 360.0 - 180.0

def wedge_halfplanes(p, theta, err=BEARING_ERR):
"""角楔 W = {X : |ang(X-p) - theta| <= err} 的两个半平面 a·X + b <= 0"""
p = np.asarray(p, float)
tm = math.radians((theta - err) % 360.0)
tp = math.radians((theta + err) % 360.0)
dm = np.array([math.cos(tm), math.sin(tm)])
dp = np.array([math.cos(tp), math.sin(tp)])
# cross(dm, X-p) >= 0 <=> a1·X + b1 <= 0
a1 = np.array([dm[1], -dm[0]])
b1 = float(-dm[1] * p[0] + dm[0] * p[1])
# cross(dp, X-p) <= 0 <=> a2·X + b2 <= 0
a2 = np.array([-dp[1], dp[0]])
b2 = float(dp[1] * p[0] - dp[0] * p[1])
return [(a1, b1), (a2, b2)]

def region_is_unbounded(svds, err=BEARING_ERR):
"""区域无界时返回一个逃逸方向（度），有界时返回 None；仅在区域非空时有意义。"""
for th in svds:
for cand in ((th - err) % 360.0, (th + err) % 360.0):
if all(abs(ang_diff(cand, t)) <= err + ANG_TOL for t in svds):
return cand
return None

def _line_intersection(P, u, Q, v):
"""直线 P + t·u 与 Q + s·v 的交点，平行时返回 None"""
det = u[0] * (-v[1]) - (-v[0]) * u[1]

16

if abs(det) < 1e-14:
return None
r = Q - P
t = (r[0] * (-v[1]) - (-v[0]) * r[1]) / det
return P + t * u

def convex_hull(P):
"""Andrew monotone chain，逆时针，去掉共线点"""
def half(pts):
st = []
for p in pts:
while len(st) >= 2 and _cross2(st[-1] - st[-2], p - st[-2]) <= 0:
st.pop()
st.append(p)
return st
P = P[np.lexsort((P[:, 1], P[:, 0]))]
lo = half(P)
hi = half(P[::-1])
return np.array(lo[:-1] + hi[:-1])

def _cross2(u, v):
return float(u[0] * v[1] - u[1] * v[0])

def _dedup_hull(cand):
"""候选顶点去重 + 凸包，返回 (P, "empty"|"degenerate"|"polygon")"""
if len(cand) == 0:
return np.zeros((0, 2)), "empty"
P = np.array(cand, float)
P = P[np.lexsort((P[:, 1], P[:, 0]))]
ded = [P[0]]
for v in P[1:]:
if np.linalg.norm(v - ded[-1]) > 1e-7:
ded.append(v)
P = np.array(ded)
if len(P) < 3:
return P, "degenerate"
H = convex_hull(P)
if len(H) < 3:
return H, "degenerate"
return H, "polygon"

# -------------------------------------------------------------------------# 区域算法 1：顶点枚举 + 凸包（主算法，精确）
# -------------------------------------------------------------------------def region_by_vertices(pts, svds, err=BEARING_ERR, tol=DIST_TOL):
"""主算法：枚举 2m 条边界直线的两两交点，用全部半平面判据筛选后求凸包。
R 是凸集，其任一极点必是两条起作用约束边界直线的交点，故枚举 + 筛选是精确的；
半平面判据在处理"边界直线过检测点"的情形时仍良定义，检测点本身也可作顶点保留。
"""
if len(pts) == 0:
return np.zeros((0, 2)), "empty"
H, lines = [], []
for p, th in zip(pts, svds):
H += wedge_halfplanes(p, th, err)
for s in (-1.0, 1.0):
a = math.radians((th + s * err) % 360.0)
lines.append((np.asarray(p, float), np.array([math.cos(a), math.sin(a)])))

17

cand = []
n = len(lines)
for i in range(n):
for j in range(i + 1, n):
X = _line_intersection(lines[i][0], lines[i][1], lines[j][0], lines[j][1])
if X is None or not np.all(np.isfinite(X)):
continue
if all(float(a @ X + b) <= tol for (a, b) in H):
cand.append(X)
return _dedup_hull(cand)

# -------------------------------------------------------------------------# 区域算法 2：半平面裁剪（独立参考实现，可直接判无界）
# -------------------------------------------------------------------------def clip_halfplane(poly, a, b, tol=DIST_TOL):
"""Sutherland–Hodgman：保留 {X : a·X + b <= 0} 的部分"""
if not poly:
return []
out = []
n = len(poly)
for i in range(n):
P = poly[i]
Q = poly[(i + 1) % n]
fp = float(a @ P + b)
fq = float(a @ Q + b)
if fp <= tol:
out.append(P)
if (fp < -tol and fq > tol) or (fp > tol and fq < -tol):
t = fp / (fp - fq)
out.append(P + t * (Q - P))
ded = []
for v in out:
if not ded or np.linalg.norm(v - ded[-1]) > 1e-9:
ded.append(v)
if len(ded) > 1 and np.linalg.norm(ded[0] - ded[-1]) < 1e-9:
ded.pop()
return ded

def region_by_clipping(pts, svds, err=BEARING_ERR, box=CLIP, tol=DIST_TOL):
"""参考实现：从大框出发逐个半平面裁剪；多边形触到外框即判区域无界。"""
poly = [np.array([-box, -box], float), np.array([box, -box], float),
np.array([box, box], float), np.array([-box, box], float)]
for p, th in zip(pts, svds):
for (a, b) in wedge_halfplanes(p, th, err):
poly = clip_halfplane(poly, a, b, tol)
if not poly:
return np.zeros((0, 2)), "empty"
P = np.array(poly, float)
if len(P) < 3:
return P, "degenerate"
if float(np.max(np.abs(P))) > box * 0.5:
return P, "unbounded"
H = convex_hull(P)
return (H if len(H) >= 3 else P), "polygon"

def region_from_bearings(pts, svds, err=BEARING_ERR):
"""主入口：返回 (顶点数组, status)，status 取 empty / unbounded / degenerate / bounded。"""
P, st = region_by_vertices(pts, svds, err)
if st == "empty":

18

return P, "empty"
rec = region_is_unbounded(list(svds), err) if len(svds) else None
if rec is not None:
return P, "unbounded"
if st == "degenerate":
return P, "degenerate"
return P, "bounded"

# -------------------------------------------------------------------------# 直径与最小包围圆
# -------------------------------------------------------------------------def _diameter_brute(P):
n = len(P)
best, pair = -1.0, (P[0], P[1])
for i in range(n - 1):
d = np.linalg.norm(P[i + 1:] - P[i], axis=1)
k = int(np.argmax(d))
if float(d[k]) > best:
best, pair = float(d[k]), (P[i], P[i + 1 + k])
return best, pair

def rotating_calipers(P):
"""凸多边形直径（旋转卡壳，O(n)）。P 需为逆时针凸序（允许共线点）。"""
n = len(P)
if n < 3:
return diameter(P, brute_limit=2)
k = 1
best, pair = -1.0, (P[0], P[1])
for i in range(n):
i2 = (i + 1) % n
e = P[i2] - P[i]
while True:
k2 = (k + 1) % n
if abs(_cross2(e, P[k2] - P[i])) > abs(_cross2(e, P[k] - P[i])):
k = k2
else:
break
for j in (k, (k + 1) % n):
d = float(np.linalg.norm(P[i] - P[j]))
if d > best:
best, pair = d, (P[i], P[j])
d = float(np.linalg.norm(P[i2] - P[j]))
if d > best:
best, pair = d, (P[i2], P[j])
return best, pair

def diameter(P, brute_limit=DIAM_BRUTE_LIMIT):
"""凸多边形直径，返回 (D, (A, B))"""
n = len(P)
if n == 0:
return 0.0, None
if n == 1:
return 0.0, (P[0], P[0])
if n == 2:
return float(np.linalg.norm(P[0] - P[1])), (P[0], P[1])
if n <= brute_limit:
return _diameter_brute(P)
return rotating_calipers(P)

19

def min_enclosing_circle(P):
"""最小包围圆 (center, radius)；顶点数很少，直接枚举 1/2/3 点候选圆。"""
P = np.asarray(P, float)
n = len(P)
if n == 0:
return None, 0.0
if n == 1:
return P[0].copy(), 0.0
cands = [(P[i].copy(), 0.0) for i in range(n)]
for i in range(n):
for j in range(i + 1, n):
c = 0.5 * (P[i] + P[j])
cands.append((c, float(np.linalg.norm(P[i] - c))))
for i in range(n):
for j in range(i + 1, n):
for k in range(j + 1, n):
A, B, C = P[i], P[j], P[k]
d = 2.0 * (A[0] * (B[1] - C[1]) + B[0] * (C[1] - A[1])
+ C[0] * (A[1] - B[1]))
if abs(d) < 1e-12:
continue
aa, bb, cc = float(A @ A), float(B @ B), float(C @ C)
ux = (aa * (B[1] - C[1]) + bb * (C[1] - A[1]) + cc * (A[1] - B[1])) / d
uy = (aa * (C[0] - B[0]) + bb * (A[0] - C[0]) + cc * (B[0] - A[0])) / d
c = np.array([ux, uy])
cands.append((c, float(np.linalg.norm(A - c))))
best = None
for c, r in cands:
if all(float(np.linalg.norm(P[i] - c)) <= r + 1e-9 for i in range(n)):
if best is None or r < best[1]:
best = (c, r)
return best

def region_area(P):
n = len(P)
if n < 3:
return 0.0
return abs(0.5 * float(np.sum(P[:, 0] * np.roll(P[:, 1], -1)
- np.roll(P[:, 0], -1) * P[:, 1])))

# -------------------------------------------------------------------------# 汇总
# -------------------------------------------------------------------------def analyse(pts, svds, err=BEARING_ERR):
"""给定检测点与示向度，输出定位区域的全套几何量（长度量按 4 位小数落盘）。"""
P, status = region_from_bearings(pts, svds, err)
base = {"n_bearings": len(pts), "status": status}
if status == "empty":
base["reason"] = ("所有示向度锥无公共交集（数据不一致：两两夹角可以 > 2°，"
"但错开的两个楔也可能不相交）")
return base
if status == "unbounded":
rec = region_is_unbounded(list(svds), err)
base["reason"] = ("示向度锥近似平行（两两夹角 <= 2°），定位区域无界，"
"直径 = +inf；必须换检测点重测")
base["recession_direction_deg"] = None if rec is None else round(float(rec), 4)
base["n_vertices"] = int(len(P))
base["vertices"] = [[round(float(v[0]), 4), round(float(v[1]), 4)] for v in P]
return base

20

if status == "degenerate" or len(P) < 3:
base["status"] = "degenerate"
base["reason"] = "公共交集退化为线段或点"
base["vertices"] = [[round(float(v[0]), 4), round(float(v[1]), 4)] for v in P]
return base
D, (A, B) = diameter(P)
c = 0.5 * (A + B)
maxr = float(max(np.linalg.norm(v - c) for v in P))
mc, mr = min_enclosing_circle(P)
base.update({
"n_vertices": int(len(P)),
"vertices": [[round(float(v[0]), 4), round(float(v[1]), 4)] for v in P],
"diameter_m": round(D, 4),
"diameter_endpoints": [[round(float(A[0]), 4), round(float(A[1]), 4)],
[round(float(B[0]), 4), round(float(B[1]), 4)]],
"circle_center": [round(float(c[0]), 4), round(float(c[1]), 4)],
"circle_radius_m": round(D / 2.0, 4),
"max_vertex_dist_m": round(maxr, 4),
"diameter_circle_covers": bool(maxr <= D / 2.0 + 1e-9),
"min_enclosing_center": [round(float(mc[0]), 4), round(float(mc[1]), 4)],
"min_enclosing_radius_m": round(float(mr), 4),
"min_circle_over_half_diameter": round(float(mr) / (D / 2.0), 6),
"area_m2": round(region_area(P), 4),
})
return base

def run_case_csv(path, truth_path=None, err=BEARING_ERR):
"""读单个算例 CSV（列含 x_m,y_m,svd_deg）与可选真值 JSON，返回 analyse 结果。"""
with open(path, encoding="utf-8") as f:
rows = list(csv.DictReader(f))
pts = [(float(r["x_m"]), float(r["y_m"])) for r in rows]
svds = [float(r["svd_deg"]) for r in rows]
res = analyse(pts, svds, err)
res["n_bearings"] = len(rows)
if truth_path and os.path.exists(truth_path):
with open(truth_path, encoding="utf-8") as f:
truth = json.load(f)
G = truth["source_xy"]
res["truth_xy"] = G
if "circle_center" in res:
res["center_error_vs_truth_m"] = round(math.dist(res["circle_center"], G), 3)
if "min_enclosing_center" in res:
res["mincircle_error_vs_truth_m"] = round(
math.dist(res["min_enclosing_center"], G), 3)
# 真值是否满足全部约束（等价于在半平面内）
H = []
for p, th in zip(pts, svds):
H += wedge_halfplanes(p, th, err)
res["truth_inside_region"] = bool(all(a @ np.asarray(G, float) + b <= 1e-6
for (a, b) in H))
return res

附录 C

问题二主要源程序

问题二的完整源程序（支撑材料 Q2/src/q2_solution.py）见下，仅保留与正文
式 (11)–(18) 直接对应的部分：交会四边形顶点、四边形直径与加权期望；寻优与等值区
域统计略去，完整脚本见支撑材料。
21

Listing 2: 问题二：期望定位区域直径与最优点、候选区域（Python 3）
import math
import numpy as np
X_MAX, ALPHA_DEG = 1500.0, 1.0 # 有效接收半径；示向度误差界
ALPHA = math.radians(ALPHA_DEG) # 探测束半张角

def quad_vertices(x_t, a, b, alpha=ALPHA):
"""S1 的两条边界射线（与 x 轴成 +-alpha）与 S2 的两条边界射线
（绕 S2->S 方向成 +-alpha）两两求交，得交会四边形 ABCD 的四个顶点。"""
s2 = np.array([a, b])
psi = math.atan2(-b, x_t - a) # S 相对 S2 的方位角
pts = []
for t1 in (alpha, -alpha):
u1 = np.array([math.cos(t1), math.sin(t1)])
for t2 in (psi + alpha, psi - alpha):
u2 = np.array([math.cos(t2), math.sin(t2)])
den = u1[0] * u2[1] - u1[1] * u2[0]
if abs(den) < 1e-14: # 两射线平行：定位区域无界
return None
s = (s2[0] * u2[1] - s2[1] * u2[0]) / den
pts.append(s * u1)
return np.array(pts)

def quad_diameter(x_t, a, b, alpha=ALPHA):
"""定位区域直径 D = 四边形顶点两两距离的最大值。"""
p = quad_vertices(x_t, a, b, alpha)
if p is None:
return math.inf
d = p[:, None, :] - p[None, :, :]
return float(np.sqrt((d ** 2).sum(-1)).max())

def dbar(a, b, n=800, alpha=ALPHA):
"""期望定位区域直径：按 P_T(x_T|S1) = 2 x_T / x_max^2 加权求和。"""
if abs(b) < 1e-9: # 两点与源点共线，区域无界
return math.inf
x_t = X_MAX * (np.arange(n) + 0.5) / n
w = 2.0 * x_t / X_MAX ** 2
d = np.array([quad_diameter(float(x), a, b, alpha) for x in x_t])
if not np.isfinite(d).all():
return math.inf
return float((w * d).sum() / w.sum())

if __name__ == "__main__": # 自检：粗网格上的最优相对位形
best = min(((dbar(x * 1.0, y * 1.0), x, y)
for x in range(950, 1251, 25) for y in range(500, 901, 25)),
key=lambda t: t[0])
print("Dbar_min = %.2f m at (a,b) = (%.0f, %.0f) m" % best)

附录 D

问题三、四主程序（待补）

问题三、问题四的策略与算法完成后，其完整可运行源程序将补充于此（对应支撑
材料 code/main.py）；该程序负责与模拟器交互，完成多源搜索、定位与清除的全过
程。
22

