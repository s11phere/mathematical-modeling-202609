% =====================================================================
%  附录（页数不限）
%  依据 2026 规范：附录必须包含支撑材料的文件列表、建模所用全部完整可运行的
%  源程序代码；如确无程序须注明“本论文没有用到程序”。
% =====================================================================
\begin{appendices}
\lstset{breaklines=true, breakatwhitespace=false, columns=fullflexible,
        keepspaces=true, showstringspaces=false, basicstyle=\ttfamily\footnotesize}

\section{支撑材料文件列表}\label{app:files}
支撑材料按问题分别存放，包含计算与绘图程序、输入算例和结果数据。
以下路径相对于附件根目录 \texttt{B\_locator/}。
\begin{enumerate}[label=\arabic*., leftmargin=2.6em, itemsep=0.12ex, topsep=0.2ex,
                  parsep=0pt, before=\sloppy]
\item \texttt{Q1/p1\_intersection.py}：问题一角楔交会、直径和覆盖判定。
\item \texttt{Q1/p1\_experiments.py}：生成五组算例、四组固定种子扫描并写出汇总结果。
\item \texttt{Q1/make\_figures.py}：重绘问题一角楔示意、算法流程和覆盖对比三幅插图。
\item \texttt{Q1/cases/}：五组输入 CSV 及真值核验文件；\texttt{Q1/results/}：逐例结果和汇总 JSON。
    \item \texttt{Q2/pysimulation/src/p2\_grid\_expectation.py}：问题二数值模拟源程序，交会四边形直径、加权期望、寻优与候选区域（附录~\ref{app:q2code}）。
    \item \texttt{Q2/MMAcode/Q2\_theory\_corrected.wl}：按理论闭式公式进行 $200$ 点中点加权求积的 Mathematica 程序，导出 $15$ m 网格上的 $E[D]$ 与最优点。
    \item \texttt{Q2/pysimulation/out/p2\_grid/p2\_grid\_map.csv}、\texttt{p2\_grid\_report.json}：数值模拟逐点结果与汇总统计（本文数值来源）。
    \item \texttt{Q2/make\_q2\_paper\_figures.py}：问题二正文插图生成脚本（由上述两份原始数据出图）。
    \item \texttt{figures/p2-mma-vs-pysim.png}：理论闭式解与数值模拟的并排对比（图~\ref{fig:p2-mma-py}）。
    \item \texttt{figures/p2-contour60.png}：$E[D]\le60$ m 等值线围出的待选区域（图~\ref{fig:p2-contour}）。
    \item \texttt{figures/p2-wedge-geometry.png}：两探测束交会与定位区域直径构造（图~\ref{fig:p2-wedge}）。
    \item \texttt{Q2/Q2modeling.md} 等：问题二建模手稿、参考热力图与 Mathematica 校验脚本。
\end{enumerate}

%问题一程序依赖 Python 3、NumPy 和 Matplotlib，在附件根目录依次运行以下命令，
%可生成五组算例、四组固定种子扫描及正文插图。
%\begin{lstlisting}[language=bash, caption={问题一数据与图形的复现命令}]
%python Q1/p1_experiments.py
%python Q1/make_figures.py
%\end{lstlisting}

\noindent\textbf{问题三附件说明。}
问题三按评审时的阅读顺序整理为以下四个目录，论文采用的最终策略为
\textrm{joint}；五代算法及其默认参数均保留，便于核对递进比较。
\begin{enumerate}[label=\alph*., leftmargin=2.6em, itemsep=0.12ex,
                  topsep=0.2ex, parsep=0pt, before=\sloppy]
  \item \texttt{Q3/code/}：五代策略、公共定位与覆盖模块、模拟器；
  \texttt{p3\_joint.py} 为最终算法，\texttt{SHA256.json} 记录执行源码校验值。
  \item \texttt{Q3/data/}：实验使用的期望场数据和先验场清单。
  \item \texttt{Q3/results/}：600 局五策略配对实验与 80 局消融的逐局汇总、
  统计结果及独立审计报告；\texttt{evidence\_map.json} 对应论文结论、公式和源码。
  \item \texttt{Q3/figures/}：五幅正文插图的 PDF、高清 PNG，
  以及包含 120 组场景的补充轨迹图册。
\end{enumerate}
\texttt{Q3/reproduce.py} 为统一复现入口；运行需要 Python 3.12 与 NumPy 2.3，
绘图另需 Matplotlib。以下命令从附件根目录执行：
\begin{lstlisting}[language=bash, caption={问题三的审计与复现入口}]
python -B Q3/reproduce.py --audit-only
python -B Q3/reproduce.py --smoke
python -B Q3/reproduce.py --out /tmp/q3-reproduction
\end{lstlisting}
第一条仅重放冻结动作并核验成本、真值访问和覆盖声明；第二条进行离线演练；
第三条在新目录重跑全部实验，保留失败与漏清局。完整动作日志和研究记录保存在
\texttt{Q3/research\_archive/} 供追溯，附件的主要阅读与复现方法见
\texttt{Q3/README.md}。附录~\ref{app:q3code} 仅展示最终策略的核心代码。

\section{问题一完整源程序}\label{app:code}
以下代码直接从对应支撑材料文件载入，各程序按所列文件名保存并保持目录结构即可运行。
\subsection{角楔交会、直径与覆盖判定}
\lstinputlisting[language=python, caption={Q1/p1\_intersection.py}]{../Q1/p1_intersection.py}

%\subsection{算例生成、随机扫描与几何校验}
%\lstinputlisting[language=python, caption={Q1/p1\_experiments.py}]{../Q1/p1_experiments.py}
%
%\subsection{问题一绘图}
%\lstinputlisting[language=python, caption={Q1/make\_figures.py}]{../Q1/make_figures.py}

\section{问题二主要源程序}\label{app:q2code}
以下列出问题二理论计算的 Mathematica 源程序。程序直接实现式~\eqref{eq:value-integral}
的中点加权求积并导出 $15$ m 网格数据；Python 数值模拟程序保留在支撑材料路径中。

\subsection{理论公式的 Mathematica 中点求积}
程序运行后在自身目录生成 \texttt{Q2\_expected\_diameter\_data.csv}。
\lstinputlisting[language={}, caption={Q2/MMAcode/Q2\_theory\_corrected.wl}]{../Q2/MMAcode/Q2_theory_corrected.wl}

\input{sections/q3-appendix.tex}

\section{问题四主程序（待补）}
问题四的定向源模型、算法与正式测试结果继续保留占位。
\end{appendices}



